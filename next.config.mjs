/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com", "via.placeholder.com", "www.google.com","lps-me.com"],
    }
};

export default nextConfig;
